# 开发续接记录（2026-08-24）

## 目标与仓库状态

- 产品目标：用户给出论文方向，智能体引导并完成论文全流程，产出含规范参考文献和交叉引用的成稿。
- 仓库：`C:\Users\欧阳威\OneDrive\桌面\新建文件夹\thesis-agent-dsh`
- 分支：`main`
- 本轮开发起点提交：`6c5718a23597c97a660e1706dcc01e34efde3ee5`
- 2026-08-26已完成最终回归与浏览器复验；本记录保留为变更和验收索引。

## 本轮已实现并完成回归的内容

1. 作者选择成为后端事实：
   - 新增环1候选题选择服务/API。
   - 多候选时未选择不能确认环1；单候选可自动确认。
   - UI 不再只写 localStorage。
2. 文献筛选与知识库登记：
   - 环3多候选文献必须保存纳入/排除决定才能确认。
   - UI 支持勾选、排除、上移、下移。
   - 纳入文献生成 RIS 题录并登记到当前任务知识库。
3. 写作质量硬 Gate：
   - 使用现有学位最低字数：本科1万、硕士3万、博士6万。
   - 校验一级章节覆盖、引用数量、结果覆盖、BOOKMARK、占位模板句和生成来源。
   - Mock/降级稿不再自动通过环6或环7。
4. 环6逐章生成：
   - 由一次性整篇请求改为逐章请求。
   - 真实模型结果账本注入正文，保留 `[RES-*]` 和 `[[BOOKMARK:*|*]]`。
   - 支持更高单章输出上限。
5. 环7逐章润色：
   - 改为逐章调用并保持事实/数字/引用指纹。
   - 最新改动加入章节级检查点；Job重试应只继续未完成章节。
   - 章节级检查点已通过单元测试和浏览器故障注入复验。
6. Token治理：
   - 空响应也记录供应商已计费 Token。
   - 默认单次预算提高到120,000，适配长论文逐章调用。
7. 失败恢复：
   - 新增安全 reopen API。
   - 环7/8失败卡提供回环6，环8还可回环3。
   - 最新改动允许“FSM FALLBACK”或“当前环存在FAILED Job”时恢复。
8. 前端交互：
   - 修复协议表单 await 后 `event.currentTarget=null` 崩溃和重复提交风险。
   - 聊天摘要随环节实时更新。
   - Job轮询从固定900ms改为指数退避，前台最多5秒、后台至少10秒。
   - 分节工作台支持批量生成和批量批准。
   - 认证模式下删除、知识库上传/下载/删除统一携带凭证。
9. 前端依赖：
   - 已把 Cytoscape 3.30.2 复制到 `ui/vendor/cytoscape.min.js`。
   - `ui/index.html` 已切换到 `./vendor/cytoscape.min.js`，浏览器确认加载成功且无控制台错误。

## 已完成的自动化验证

- 新增 `tests/test_full_flow_hardening.py`，覆盖作者选题、文献筛选入库、降级稿拒绝、结果账本注入和环8恢复。
- 最终完整回归：`199 passed, 1 warning in 62.24s`。
- 环7检查点定向回归覆盖“前两章保存、第3章失败、重试只生成剩余章节”。
- 旧`[L*]`兼容链路会生成连续编号、引用映射和规范参考文献节；越界`[Lx]`会被质量门禁拒绝。

## 第二次真实浏览器复验结果

隔离任务：`TASK-55FE72D555C8`，本科论文方向。

- 环1：选择前确认按钮禁用；选择后端保存的题目与UI一致。
- 聊天摘要：确认后正确显示“完成1/10、当前环2”。
- 环3：保存前不能确认；24条纳入文献成功登记到任务知识库。
- 环1至环5均使用真实模型通过。
- 环6严格Gate通过：
  - 5章
  - 15,281字
  - 15个唯一 `[L*]` 引用
  - 0处已知占位模板句
  - Job记录35,402 Token
- 环7在旧运行进程中曾因整环重试重复已完成章节，三次尝试后累计115,983 Token并被120,000预算硬停；该行为现已由章节级检查点修复。
- 公网 Cytoscape CDN失败已通过本地vendor依赖修复。

## 2026-08-26 隔离浏览器最终复验

- 任务：`TASK-DD3CF45F8B30`，本科。
- 10环逐环执行、逐环确认，最终进度100%，10/10环均为`PASSED`。
- 环6：5章、14,212字。
- 环7：在第3次调用注入失败，最终Job为`SUCCEEDED`、`attempt=2`、总调用6次、总Token 600；验证只续写未完成章节。
- 环8：3条引用全部通过并建立`[L1]→[1]`、`[L2]→[2]`、`[L3]→[3]`映射。
- 环9：生成DOCX 24,375字节，文件签名`PK`，版式检查0硬伤。
- 环10：最终确认完成；浏览器控制台0 error、0 warning。

## 后续阶段建议

1. 用真实模型重新执行完整10环长论文复验，重点记录环6/7逐章质量、Token峰值和失败恢复。
2. 对检索并下载的真实全文做相关性、可访问性和元数据一致性抽检。
3. 增加长时间、并发任务、Worker重启和数据库迁移压力测试。
4. 继续建设结构化表格/图片、复杂公式和学校模板样式差异诊断。

## 重要约束

- 不输出或修改 `.env` 中的真实模型密钥。
- 不把降级/Mock稿件标为通过。
- 不为了跑通演示绕过引用、字数、结果或DOCX校验。
- 不使用 `git reset --hard` 或覆盖当前未提交改动。
