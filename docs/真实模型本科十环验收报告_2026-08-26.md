# 真实模型本科十环验收报告（2026-08-26）

## 验收范围

- 模型：项目现有DeepSeek真实账号与`deepseek-v4-flash`。
- 任务：本科，`TASK-6B06C7D92E30`。
- 原始方向：生成式人工智能辅助学术写作中的引用可信性保障机制研究。
- 作者验收脚本显式选择候选题并完成文献筛选、逐环确认，不绕过任何Gate。
- 任务、FSM、知识库、Job和DOCX全部使用临时隔离目录。
- Mock回退关闭；RAG模型下载关闭；文献联网检索开启。

## 最终结果

- 十环状态：10/10 `PASSED`，浏览器进度100%。
- 环6最终稿：5章、11,080字；成功作业12,523 Token。
- 环7：5章、11,120字；成功作业39,646 Token。
- 环8：8条题录全部验证通过，生成8条连续编号参考文献，无`[L*]`残留。
- 环9/10：排版与交付控制流通过。
- 浏览器：十环、任务列表、知识库状态一致；控制台0 error、0 warning。
- 完整实验累计187,274 Token，包含三次用于暴露问题的失败作业；费用单价未配置，因此费用不可观测，不能解释为0元。

## 真实失败与修复

| 现象 | 证据 | 修复 |
| --- | --- | --- |
| 环6整环重试重复章节 | 首次85,987 Token后预算阻断 | 环6增加章节级持久化检查点 |
| 长结构化调用最终正文为空 | 恢复作业20,227 Token后仍返回空内容 | 按DeepSeek官方协议，结构化JSON默认关闭高思考模式，并记录`finish_reason`、推理长度和用量的脱敏诊断 |
| 全文只有8,654字 | 5章均生成但低于本科1万字 | 单章保存前检查最低长度，最多一次完整章节纠偏；不达标检查点会重写 |
| DOCX正文折叠为单个模板段落 | 首版仅9个非空段落 | 内置模板将Markdown转换为Word标题/正文/参考文献；复生成后129段、9个一级标题、27个二级标题、8条独立参考文献 |
| 环9可能跨任务猜DOCX | 全局`*_thesis_render.docx` glob兜底 | 删除全局glob，只接受当前任务记录的路径或精确文件名 |

## 学术质量发现

首次环3检索的26条候选中，验收脚本按“有DOI且题录真实”纳入8条，实际混入工业零件检测、地基检测、环境法等明显偏题文献。环8验证的是“题录真实”，不能证明“与论文相关”。因此本次只能证明控制流和题录真实性链路通过，不能把首版正文判为学术质量通过。

已新增：

- 基于选题、学科和实际检索式的中英文词法相关度排序；
- 相关度百分比和命中词进入环3产物与UI；
- 自动验收脚本只纳入“有DOI、题录可靠、相关度达标”的候选；不足3条立即停止。

新排序已使用首轮26条真实候选离线验证：毕业论文与抄袭检测文献进入前列，工业/地基/自然场景检测被降序。但为了避免继续产生无必要模型费用，本轮没有从环3重新生成整篇论文，因此相关度修复仍需下一次真实任务闭环确认。

## 判定

- 十环控制流、Job预算、失败恢复、引用编号和浏览器状态：**通过**。
- DOCX语义结构修复后的离线复生成：**通过**。
- 首轮论文的文献相关性与学术内容质量：**不通过**。
- 产品整体：继续保持“受控试用 / 条件通过”，不能宣称无人审查即可稳定交付完美论文。

官方思考模式说明：https://api-docs.deepseek.com/guides/thinking_mode
